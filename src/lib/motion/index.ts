/**
 * Motion system — barrel export.
 * Import everything from `@/lib/motion`.
 */

export { duration, easing, spring, delay } from "./tokens";
export { motionTheme } from "./themes";
export {
  fadeIn,
  fadeInUp,
  fadeInScale,
  slideIn,
  scalePress,
  staggerContainer,
  staggerItem,
  expandCollapse,
} from "./variants";
